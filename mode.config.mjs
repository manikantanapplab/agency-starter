// ============================================================
// CSS MODE — change this ONE line per project
// ============================================================

export const MODE = 'single'; // 'single' | 'per-page' | 'per-component'

// single        → one app.css for all pages
//                 best for: small/medium sites, quick deadlines
//
// per-page      → separate CSS per page (index.css, about.css...)
//                 best for: large sites, pages use different components
//
// per-component → base.css always + component CSS per component
//                 best for: Drupal/WP/Laravel/.NET, editors move components freely
